# Session artifacts (September 2026)

Working notes from the 9–23 September 2026 pass. These are **criteria and referee notes**, not a Clay solution.

- `Frohman_What_Is_Actually_Proved_2026.tex` — Def 7.1 implies global smoothness; that is not (A) or (B).
- `Lean_NS_Module_Referee_2026.tex` — the supplied Lean module does not formalize NS or Hodge.
- Larger geometric memoirs from the same session tree are added in following commits if the push succeeds.

PDFs remain local (`/home/workdir/artifacts/`). Compile with `pdflatex` twice.
