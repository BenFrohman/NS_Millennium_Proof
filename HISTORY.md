# Project History & Priority Claim

**Project:** NS_Millennium_Proof — Frohmanian Symplectic Tether Framework  
**Author:** Benjamin Stanley Frohman (X.com : Investor0x / Bit21)  
**Primary Development Period:** February–June 2026

## Key Milestones

- **2026-02 to 2026-04**: Initial development of the symplectic/holographic dual approach and early tether constructions (living LaTeX document).
- **2026-04-02**: Major LaTeX manuscript ("The Frohman Symplectic and Holographic Dual") finalized with novel conjectures on turbulence singularity.
- **2026-05**: Lean 4 formalization begins. Core geometric layer (ForMathlib.NS.Tether) developed with full 1st-principles derivations, degeneracy lemmas, and the 5-step uniqueness proof for the minimal correction.
- **2026-05-28 to 2026-05-31**: Complete implementation versions stabilized in backup folders.
- **2026-06 to 2026-07**: Recovery from build corruption event. Authorship headers and project-level files (COPYRIGHT.md, AUTHORS.md, HISTORY.md) applied to all original modules.
- **2026-08-26 (WIP)**: Kernel-path restoration of the full restored modules: Lean 4 `kappa` / `C_CZ(3)`, canonical `TetherKernel`, BKM criterion, uniqueness types, quartic Young absorption, and top-level `Modules/` copies. Labeled WIP under Benjamin Stanley Frohman. `lake build NS_Millennium_Proof ForMathlib` succeeds with documented classical `sorry`s.
- **2026-08-26 (attribution)**: Mathlib five-line `Authors: Benjamin Stanley Frohman` headers on original Lean modules; `.mailmap` maps commit aliases `Frohmanian` / `BenFrohman` without rewriting history; PR `Author:` comments for Lean/Zulip contact.
- **2026-08-26 (provenance, branch `kernel/cohesive-tether-kappa-bkm`, PR #8)**: `d23e11e` attribution; `a256cb0` Kato→BKM composition; `deff823` Lyapunov Young composition; `3ae8a10` Fin 3 vs ℝ and ε-Young; `1c76f50` Jacobi `jacobiator = 0` (Lean 4, not True.intro). Author: Benjamin Stanley Frohman (`frohmanbenjamin@gmail.com`). GitHub: @BenFrohman. X.com : Investor0x.
- **2026-08-26 (classical boxes, same branch/PR)**: `496c167` `div_smul` (unconditional, `fderiv_const_smul_field` + `Finset.mul_sum`); `curl_gradient` from mixed partials (`IsSymmSndFDerivAt`); `Tstar` as `EReal` supremum of existence times (no `sorry` def); CoadjointOrbit is the divergence-free subtype; `Pi_u_zero`; energy-zero Euler branch when `u ≡ 0`. Remaining Kato/BKM/stretching/Jacobi sorrys are untranscribed steps in this authorship, not community fill-ins. Author: Benjamin Stanley Frohman.
- **2026-08-26 (certificate path)**: Replace remaining `sorry` *definitions* with real objects: Gâteaux `FunctionalDerivative` (choice of representative), integral Biot–Savart kernel, Gaussian mollifier / convolution, `ComparisonODE` as choice of a C¹ Riccati solution. Live `#print axioms tetherKernel_C3` has **no `sorryAx`**. `comparison_ode_nonneg` closed. Existence of the majorant ODE remains the named theorem `comparison_ode_exists`. The Lean certificate (`#print axioms frohmanian_tether_theorem` without `sorryAx`) is the end goal. Author: Benjamin Stanley Frohman.
- **2026-08-26 (Picard local majorant)**: `comparison_ode_local_exists` kernel-closed via mathlib Picard–Lindelöf (`of_contDiffAt_one` on the polynomial field). Live `#print axioms` has no `sorryAx`. Global `comparison_ode_exists` remains the continuation step. Author: Benjamin Stanley Frohman.
- **2026-08-26 (Lean 4 types)**: `CoadjointOrbit` is a Lean 4 `public structure … where` (Language Reference 4.4.2; `structure … :=` deprecated since v4.14). `@[expose]` is `def`-only. Kernel path has no Lean 3 `begin`/`end`, `have …, from`, or `structure :=`. Author: Benjamin Stanley Frohman.
- **2026-08-26 (close boxes after defs)**: Equilibria of the Riccati ODE (`y0 = 0` and `y0 = C/κ''`) are constant global solutions; `BiotSavart_zero`; `velocity_from_zero_orbit`; `comparison_majorant_global_bound` glue. Interior continuation of `comparison_ode_exists`, Kato, BKM, stretching, Jacobi remain. Author: Benjamin Stanley Frohman.
- **2026-08-26 (assembly)**: C1 for `TetherKernel` (`tether_coadjoint_invariance`); `IsComparisonODESolution` as Lean 4 `structure where`; `global_regularity_for_NS` existence from `global_regularity` (uniqueness of the Cauchy problem remains); blueprint Picard existence wired to `comparison_ode_exists`. Main theorem still `sorryAx`. Author: Benjamin Stanley Frohman.
- **2026-08-26 (disclaimer)**: README, COPYRIGHT, AUTHORS, CONTRIBUTING, and CITATION.cff record that the Lean encoding was compiled with an AI assistant (Grok, xAI) from Benjamin Stanley Frohman's paper proofs; authorship of the mathematics remains Frohman's.
- **2026-08-26 (κ is not 1)**: `C_CZ(3)` is the spherical L¹ of the Constantin–Fefferman / Majda–Bertozzi strain kernel `(3/(8π))[(z×ω)⊗z + z⊗(z×ω)]/|z|^5`. Lean now defines `CalderonZygmundConstant3D := (3/(8π)) · 4π`, proves `= 3/2` and `≠ 1`, and sets `κ = C_CZ(3)` by (C3). The stand-in `:= 1` is removed. Live `#print axioms` of `kappa_eq_three_halves` / `kappa_ne_one` / `kappa_pos` has no `sorryAx` (only `propext`, `Classical.choice` from `Real.pi`, `Quot.sound`). Young remains homogeneous in this scale (`κ' = (3/4)κ`). Author: Benjamin Stanley Frohman.
- **2026-08-26 (energy hypothesis / no open TODOs)**: `global_regularity_for_NS` uses the classical Kato/Leray finite kinetic energy `Integrable (fun x => ‖u₀ x‖^2)`. That is the same energy condition as an earlier `HasFiniteIntegral` encoding of the finite-integral conjunct, not a second theorem. Removed `True` placeholder theorems and contributor-facing TODO language; remaining Lean transcription is author-only. Author: Benjamin Stanley Frohman.
- **2026-08-26 (GitHub Community / Lean community)**: GitHub Community is the health checklist (`CONTRIBUTING.md`); Contributors graph and Lean Zulip/`Authors:` name Benjamin Stanley Frohman / @BenFrohman only. AI encoding is disclosure, not `Co-authored-by` and not an `Authors:` name. Author: Benjamin Stanley Frohman.
- **2026-08-26 (X.com : Investor0x)**: Public files label Investor0x as `X.com : Investor0x` (link `https://x.com/Investor0x`), never as a GitHub `@` handle. GitHub remains @BenFrohman. Author: Benjamin Stanley Frohman.
- **2026-08-26 (majorant ODE on t ≥ 0)**: `IsComparisonODESolution` and the Riccati comparison lemmas are typed on the paper interval `t ≥ 0` (for `y0 > C/κ''` the field can blow up in finite negative time). Equilibria remain closed. Interior inversion of `majorantSeparable` is the named remainder. Author: Benjamin Stanley Frohman.
- **2026-08-26 (ContDiff.eventually pin)**: `directionalCoord_pressureGradient` uses `ContDiffAt.eventually` (`n ≠ ⊤`) then `differentiableAt_one`. Bare `∞` is `ℝ≥0∞`, not `WithTop ℕ∞`. No `DifferentiableAt.eventually`. Author: Benjamin Stanley Frohman.
- **2026-08-26 (uniqueness by polarization)**: `uniqueness_of_minimal_tether` is kernel-closed (`#print axioms` has no `sorryAx`): a correction that saturates the C3 quadratic form and polarizes equals `TetherKernel` (`linarith` on the polarization identity). This is the paper's *minimal* uniqueness, not the false claim that every antisymmetric map is the kernel. Live `lake build` succeeds. Kato/BKM/CZ/Jacobi/Hölder/hIdent/hRiccati remain named remainders; the Clay main theorem still has `sorryAx`. Author: Benjamin Stanley Frohman (GitHub @BenFrohman; X.com : Investor0x).
- **2026-08-26 (simp vs simpa)**: Equilibrium init steps use `simp [h0]` / `simp [hstar]` (`linter.unnecessarySimpa`: `simp` already closed). Keep `simpa … using e` where a term is rewritten then applied. Lean 4.30.0 `Init/Tactics.lean`. Author: Benjamin Stanley Frohman.
- **2026-08-26 (clip bound)**: `|z| ≤ Y+1` for `z = max(-1, min(x, Y+1))` uses the explicit chain `1 ≤ Y+1` from `0 ≤ Y`, hence `-(Y+1) ≤ -1 ≤ z`. Not `le_add_of_nonneg_right hY` at `a = 0` (that would only give `-Y ≤ 0`). Kernel-checked. Author: Benjamin Stanley Frohman.
- **2026-08-26 (global majorant existence, Lean 4.30.0-rc1)**: Interior `comparison_ode_exists` is kernel-closed. The cubic field is Lipschitz on bounded intervals by the mean-value bound on `f' = 2Cy − 3κ''y²` (`Convex.lipschitzOnWith_of_nnnorm_deriv_le`, Language Reference tactics / Mathlib MeanValue). Compact cutoff `majorantFieldClip` is globally Lipschitz; Picard–Lindelöf on every slab `Icc (-1) (T+1)` (`IsPicardLindelof.of_time_independent`); Grönwall uniqueness (`ODE_solution_unique_of_mem_Icc`); slabs glue to a global C¹ curve; barrier trapping in `[0, max(y0, C/κ'')]` recovers the unclipped field. Live `#print axioms TetheredLyapunov.comparison_ode_exists` is `propext`, `Classical.choice`, `Quot.sound` — **no `sorryAx`**. Syntax is Lean 4 only (`theorem … := by`, `structure … where`, `module` / `public`, no `begin`/`end`, no `have …, from`). `lake build` succeeds. The main Clay theorem still has `sorryAx` on Kato/BKM/stretching/Jacobi transcription. Author: Benjamin Stanley Frohman (GitHub @BenFrohman; X.com : Investor0x).
- **2026-08-27 (kernel-density uniqueness + C2, Lean 4.30.0-rc1)**: Reconstruction Lemmas 2.3.1–2.3.3 as `HasTetherKernelDensity` / `canonicalTetherDensity` (`α = -κ |ω|²`). Live `#print axioms uniqueness_of_kernel_density`, `uniqueness_of_kernel_density_fun`, and `tetherKernel_has_canonical_density` are `propext`, `Classical.choice`, `Quot.sound` — **no `sorryAx`**. C2: `FunctionalDerivative_eq_zero_of_not` (ASCII binder `dH`; the notation `δ F /δω` is illegal in binders); `tetherKernel_C2_of_no_gateaux`; finite-energy `tetherKernel_degenerates_on_kinetic_energy` (`Π_u u = 0` or integrable energy-zero AE vanish; paper Lemma 2.3.2). `Pi_u_of_energy_zero` records the degenerate Gram–Schmidt `a/0 = 0`. Main theorem 4th conjunct is density uniqueness. `frohmanian_tether_theorem` still has `sorryAx` (Kato/BKM/CZ/Jacobi/hIdent/hRiccati/δH=u/div Biot–Savart). Author: Benjamin Stanley Frohman (GitHub @BenFrohman; X.com : Investor0x).
- **2026-08-27 (Biot–Savart algebra + `div curl = 0`)**: `cross` is bilinear (`cross_smul_left/right`, `cross_add_left/right`). `BiotSavart_smul` is homogeneous; `BiotSavart_add` is additive on integrable kernel densities. Pointwise `hasDerivAt_half_norm_sq` is the kinetic-energy Gâteaux seed (`d/dt |_{0} |a+tb|²/2 = ⟨a,b⟩`). `div_curl` / `div_of_eq_curl` are mixed-partials identities in the same style as `curl_gradient` (no `sorryAx`). `div_biot_savart_velocity` remains the named remainder that Biot–Savart is a curl; `functional_derivative_of_kinetic_energy` remains the Lie-algebra pairing identification `δH = u`. Author: Benjamin Stanley Frohman (GitHub @BenFrohman; X.com : Investor0x).
- **2026-08-27 (product-rule divergence + kinetic-energy variation)**: `div_smul_field` is `div(φ V) = ∇φ · V + φ div V` (no `sorryAx`). `BiotSavart_affine` is `K(ω + t ε) = Kω + t Kε` on integrable kernels. `pairing_cross_self_right` is `r · (ω × r) = 0`. `hasDerivAt_half_norm_sq_at` at arbitrary time. `kineticEnergy_hasDerivAt_velocity_pairing` differentiates `∫ |a+tb|²/2` under the integral (mathlib parametric dominated convergence) and yields `∫ ⟨u, δu⟩` — the paper's `δH = u` in the Lie-algebra pairing. `functional_derivative_of_kinetic_energy` remains the vorticity-pairing identification. Author: Benjamin Stanley Frohman (GitHub @BenFrohman; X.com : Investor0x).
- **2026-08-27 (Biot–Savart `div(ω × z) = 0` + degree-2 density)**: `cross_coord`, `div_cross_right`, `div_cross_offset` (no `sorryAx`). `biotSavartKernel_of_ne`. `functional_derivative_eq_velocity_of_unique_repr` (choose glue). `step2_degree_of_canonical_density` is Reconstruction 2.3.1 with frozen Gâteaux slots. Live `#print axioms` has no `sorryAx`. Main theorem still `sorryAx`. Author: Benjamin Stanley Frohman (GitHub @BenFrohman; X.com : Investor0x).
- **2026-08-27 (step2 for `TetherKernel`; Euler energy with convective IBP)**: `step2_degree` / `step5_higher_order` are `TetherKernel` specializations of the canonical-density theorem (frozen Gâteaux / `Π_u`); antisymmetry of an arbitrary `B` does not force degree two. `integration_by_parts_of_vanishing_flux`; pointwise convective integrand `⟨u,(u·∇)u⟩ = ⟨u,∇(½|u|²)⟩`; `convective_energy_pairing_vanishes` and `pressure_energy_pairing_vanishes` are the paper's two IBP cancellations. `euler_energy_conservation` composes Euler momentum + those two IBPs + the parametric energy derivative. `majorant_comparison_principle` is the closed Riccati barrier (`comparison_ode_stability`) with the NS inequality as a named hyp. Main theorem still `sorryAx`. Author: Benjamin Stanley Frohman (GitHub @BenFrohman; X.com : Investor0x).
- **2026-08-27 (C¹ IBP vs C² mixed partials, correct slots)**: Energy identities are C¹ (`DifferentiableAt` of the field implies coordinate C¹ and C¹ of `½|u|²`). Mixed partials stay C² (`ContDiffAt ℝ 2` / `IsSymmSndFDerivAt`) on `div_curl`, `curl_gradient`, `div_of_eq_curl`. `euler_energy_conservation` now *applies* `convective_energy_pairing_vanishes` and `pressure_energy_pairing_vanishes` (does not take their conclusions as opaque hyps). Biot–Savart `div u = 0` is `div_biot_savart_of_eq_curl` (C² curl identification), not flux IBP. Vorticity pressure is `curl ∇p = 0`, not `∫⟨u,∇p⟩`. Author: Benjamin Stanley Frohman (GitHub @BenFrohman; X.com : Investor0x).
- **2026-08-27 (Biot–Savart integrand divergence off the pole)**: `div_biotSavart_integrand` is kernel-closed (`#print axioms` no `sorryAx`): C¹ product rule on `K(z,y)(ω(y)×(z−y))`, `∇K ∥ (z−y)`, triple product `r·(ω×r)=0`, and `div(ω×(z−y))=0`. Mixed partials not used. Interchange of `div` and the Biot–Savart integral remains the named remainder for `div_biot_savart_velocity`. Author: Benjamin Stanley Frohman (GitHub @BenFrohman; X.com : Investor0x).
- **2026-08-27 (a.e. integrand div + interchange glue)**: Haar on `ℝ³` is non-atomic, so `{x}` is null and `div_biotSavart_integrand_ae` holds. `div_BiotSavart_of_interchange` closes `div (BiotSavart ω) x = 0` once `div` and the integral interchange. Live `#print axioms` has no `sorryAx`. `div_biot_savart_velocity` remains the interchange (or C² curl) remainder. Author: Benjamin Stanley Frohman (GitHub @BenFrohman; X.com : Investor0x).
- **2026-08-27 (paper §2.1 stretching + three-term curl)**: `directionalCoord_convective` is the product rule. `curl_convective` is the C² identity `curl((u·∇)u) = (u·∇)ω − (ω·∇)u + (div u)ω`; `curl_convective_div_free` drops the last summand when `div u = 0`. `curl_add3` uses `hbc` (C¹ of `v+w` coordinates), `add_assoc` on `EuclideanSpace` (`AddCommGroup` / `PiLp.add_apply` on each slot), and checks curl components `0,1,2` from `curl_apply` + `hdir`. Live `#print axioms curl_add3` / `curl_convective` / `curl_convective_div_free` are `propext`, `Classical.choice`, `Quot.sound` — **no `sorryAx`**. Time/space Schwarz and `curl Δ = Δ curl` remain named remainders for assembling `vorticity_transport`. Author: Benjamin Stanley Frohman (GitHub @BenFrohman; X.com : Investor0x).
- **2026-08-27 (Schwarz spacetime + curl of time derivative)**: `fderiv_prod_snd_slice` / `deriv_prod_fst_slice` are the product-space slices. `time_space_mixed_partials` is Schwarz (`IsSymmSndFDerivAt` on `(t,x) ↦ f t x`). `curl_time_deriv` is `curl ∂t u = ∂t curl u` on components `0,1,2`. `laplacian_coord` is the scalar trace of the Hessian on each slot (`PiLp.proj` / `map_sum`). Live `#print axioms` of those four has **no `sorryAx`**. `curl_laplacian` and assembly of `vorticity_transport` remain. Author: Benjamin Stanley Frohman (GitHub @BenFrohman; X.com : Investor0x).

This formalization establishes priority for the **Frohmanian Symplectic Tether Regularity Theorem** as the original solution approach to the 3D Navier–Stokes Millennium Problem.

All original mathematical content (especially the tether kernel, projected bilinear correction, and associated 2-form) is the work of Benjamin Stanley Frohman.

For formal submission or priority claims, this dated record, combined with git history and the dated LaTeX manuscript, serves as primary evidence.

